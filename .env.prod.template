# 生產環境變數配置範本
# Production Environment Variables Template
# 複製此檔案為 .env.prod 並填入實際值

# ===========================================
# 資料庫配置 - Database Configuration
# ===========================================
DB_PASSWORD=your_secure_mysql_password_here
# 建議使用至少16字元的強密碼，包含大小寫字母、數字和特殊字元

# ===========================================
# Redis 配置 - Redis Configuration  
# ===========================================
REDIS_PASSWORD=your_secure_redis_password_here
# Redis 密碼，建議使用隨機生成的強密碼

# ===========================================
# JWT 安全配置 - JWT Security
# ===========================================
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long_and_random
# JWT 簽名密鑰，必須至少32字元長，建議使用隨機生成的字串
# 範例生成指令: openssl rand -base64 32

# ===========================================
# 通知服務配置 - Notification Services
# ===========================================
# Firebase Cloud Messaging (推播通知)
FCM_API_KEY=your_firebase_cloud_messaging_api_key

# SMTP 電子郵件服務配置
SMTP_HOST=smtp.gmail.com
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_email_app_password
# 注意：使用 Gmail 時需要設定應用程式密碼

# ===========================================
# SSL/TLS 憑證配置 - SSL/TLS Certificates
# ===========================================
SSL_CERT_PATH=/etc/ssl/certs/dating-app.crt
SSL_KEY_PATH=/etc/ssl/private/dating-app.key
# SSL 憑證檔案路徑，用於 HTTPS 連線

# ===========================================
# 外部服務配置 - External Services
# ===========================================
# 監控服務
PROMETHEUS_PASSWORD=your_prometheus_admin_password
GRAFANA_ADMIN_PASSWORD=your_grafana_admin_password

# ===========================================
# 部署配置 - Deployment Configuration
# ===========================================
DOMAIN_NAME=your-domain.com
# 你的應用程式域名

BACKUP_S3_BUCKET=your-backup-bucket-name
BACKUP_S3_ACCESS_KEY=your_s3_access_key
BACKUP_S3_SECRET_KEY=your_s3_secret_key
# 用於自動備份的 S3 配置

# ===========================================
# 安全配置 - Security Configuration
# ===========================================
RATE_LIMIT_REDIS_KEY_PREFIX=rate_limit
SECURITY_SALT=your_random_security_salt_32_chars
# 安全相關的隨機鹽值

# ===========================================
# 日誌與監控 - Logging and Monitoring
# ===========================================
LOG_LEVEL=info
SENTRY_DSN=your_sentry_dsn_for_error_tracking
# 錯誤追蹤服務 DSN (可選)

# ===========================================
# 效能調優 - Performance Tuning
# ===========================================
MAX_WORKERS=8
GOMAXPROCS=8
# Go 應用程式的併發處理設定

# ===========================================
# 生成安全密鑰的指令範例
# Example Commands for Generating Secure Keys
# ===========================================
# 生成 JWT 密鑰: openssl rand -base64 32
# 生成隨機密碼: openssl rand -base64 24
# 生成安全鹽值: openssl rand -hex 16